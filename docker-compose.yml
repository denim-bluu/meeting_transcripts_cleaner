services:
  app:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: meeting-transcript-app
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
